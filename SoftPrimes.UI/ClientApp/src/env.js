(function (window) {
  window.__env = window.__env || {};

  // API url
  window.__env.apiUrl = '';

  // Apps Url
  window.__env.iosUrl = 'https://www.apple.com/app-store/';
  window.__env.androidUrl = 'https://play.google.com/store';

  // Whether or not to enable debug mode
  // Setting this to false will disable console output
  window.__env.enableDebug = true;
}(this));
